import 'package:flutter/material.dart';

class Guard extends StatelessWidget {
  const Guard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Page Guard"),
    );
  }
}
